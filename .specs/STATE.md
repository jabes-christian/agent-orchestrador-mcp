# Project State — agent-orchestrator-mcp

> Memória do projeto entre fases e sessões. Ver `references/memory.md` da skill `tlc-spec-driven`
> para o protocolo de leitura/escrita deste arquivo.

## Decisions

| ID | Decisão | Status | Contexto |
| --- | --- | --- | --- |
| AD-001 | Transporte MCP no fio interno do compose é **Streamable HTTP**. Cada MCP server oficial (stdio-only na forma distribuída) roda em seu próprio container atrás de um shim FastMCP (`create_proxy`) que traduz stdio→Streamable HTTP. O transporte SSE/HTTP+SSE nunca é usado — está deprecado na spec MCP (revisão 2025-11-25). | active | `.specs/features/mcp-orchestrator/design.md` §1.2 |
| AD-002 | O grafo do orquestrador é um `StateGraph` **customizado** do LangGraph (nós/arestas próprios), não um agente prebuilt (`create_agent`/ReAct pronto). Necessário para controle explícito de `iterations`, `trace.steps` por chamada e bloqueio pré-execução da allowlist. | active | `.specs/features/mcp-orchestrator/design.md` §1.1 |
| AD-003 | Acesso ao LLM via OpenRouter usa o pacote dedicado `langchain-openrouter` (`ChatOpenRouter`), não `ChatOpenAI` com `base_url` sobrescrito — a doc oficial do LangChain desaconselha essa rota para providers não-OpenAI. | active | `.specs/features/mcp-orchestrator/design.md` §1.6 |
| AD-004 | A suíte de avaliação de roteamento (MCPO-10) roda em dois modos: modo CI (default, offline, fixtures de resposta do LLM gravadas uma vez) dentro do gate de PR; modo live (`EVAL_LIVE=1`, chamada real ao OpenRouter) fora do gate. Mudar o prompt de decisão exige regravar as fixtures na mesma task. | active | `.specs/features/mcp-orchestrator/design.md` §1.5 |
| AD-005 | Nenhum nome de MCP server ou de tool pode aparecer hardcoded fora de `config/servers.yaml` + `src/orchestrator/mcp_client/registry.py`. Sustenta MCPO-02 AC4 e o Success Criterion "adicionar um terceiro server não exige alterar o pacote do grafo". | active | `.specs/features/mcp-orchestrator/design.md` §1.3 |
| AD-006 | MCPO-12 (limite de custo/tokens por request) é adiado formalmente para v1.1 e não vira task nesta feature. O AC do requisito exige `error.code = "TOKEN_BUDGET_EXCEEDED"`, código ausente do catálogo fechado de 8 erros já validado na Fase 3 (spec); reabrir esse contrato de API por um requisito P3 não se paga. Os limites já existentes (`MAX_REACT_ITERATIONS=5`, `REQUEST_TIMEOUT_S=120`) já fornecem um teto grosseiro de custo por request. | active | `.specs/features/mcp-orchestrator/spec.md` → Requirement Traceability (MCPO-12, status `Deferred`); `.specs/features/mcp-orchestrator/tasks.md` → Requisitos fora do escopo destas tasks |
| AD-007 | `mcp_client.registry` não conhece `tool_policy.yaml` — `ServerInfo.tools[].write` que ele produz é um placeholder `False`, documentado inline. A classificação real de escrita só existe em `mcp_client.policy` (T10). `GET /servers` (T15) é o único ponto autorizado a sobrescrever esse placeholder com `policy.is_write()` antes de responder ao cliente, conforme MCPO-02 AC3 exige `write: bool` correto. Corrigido em `tasks.md` (T15 ganhou `Depends on: T10`) durante a revisão do Lote 1 do Execute, antes de T15 ser implementada no Lote 2 — nenhum código já commitado precisou mudar. | active | `.specs/features/mcp-orchestrator/tasks.md` → T15; `src/orchestrator/mcp_client/registry.py` (comentário inline) |
| AD-008 | `graph.nodes.tools` (T21) não levanta exceção quando uma falha de transporte persiste após o retry — preenche `error: ErrorInfo` no estado retornado em vez disso. Motivo: LangGraph só mescla ao estado o que um nó *retorna*; levantar exceção (como `agent` faz para `LLM_PROVIDER_ERROR`) perderia os `steps` já acumulados nessa chamada do nó, violando MCPO-04 AC2 ("registrar a tentativa falha em trace.steps"). Isso diverge do diagrama de `design.md` §1.2, que desenha `tools` com uma única aresta de saída (de volta a `agent`) — `graph/builder.py` (T24) precisa de uma aresta condicional extra depois de `tools` (`error` preenchido → `finalize`), simétrica à aresta `guard --deny--> finalize` já desenhada no diagrama original. Encontrado durante a implementação de T21 no Lote 3 do Execute; `design.md` §1.2 ainda não foi atualizado com a aresta nova — fica para quando T24 montar o `StateGraph`. | active | `src/orchestrator/graph/nodes.py` (comentário `SPEC_DEVIATION` inline, T21); `.specs/features/mcp-orchestrator/design.md` §1.2 (diagrama a complementar em T24) |
| AD-009 | `api.routes_tasks` (T25) invoca o grafo via `graph.astream(..., stream_mode="values")`, guardando o último snapshot de estado emitido a cada passo, em vez de `graph.ainvoke(...)` como `design.md` §1.5 nomeava literalmente. Motivo: a mesma seção exige que a resposta `REQUEST_TIMEOUT` (504) carregue "o trace parcial acumulado até o ponto do cancelamento (steps já executados continuam no trace)" — cancelar um `ainvoke()` não devolve nenhum estado parcial, as duas frases eram logicamente incompatíveis. Confirmado com o usuário durante a implementação de T25 (Lote 4): optou por manter a exigência de trace parcial (peso de contrato, MCPO-04 AC1/AC2) e corrigir a menção a `ainvoke` em vez do caminho oposto. `design.md` §1.5 e a tabela de Error Handling Strategy já foram corrigidas para nomear `astream`. | active | `.specs/features/mcp-orchestrator/design.md` §1.5, Error Handling Strategy; `src/orchestrator/api/routes_tasks.py` (comentário `SPEC_DEVIATION` inline, T25) |
| AD-010 | Construir um `ChatOpenRouter` real (`llm.provider.get_chat_model`, que internamente instancia `openrouter.sdk.OpenRouter`) em um event loop asyncio e depois usar QUALQUER outro recurso `asyncio`/ASGI em um event loop diferente, no mesmo processo, corrompe esse segundo loop (sintoma: `ERROR: ASGI callable returned without completing response.` seguido de hang indefinido em um servidor `fastmcp`/`uvicorn` completamente não relacionado). Reproduzido isoladamente (fora do pytest) com um script mínimo: a falha desaparece ao trocar `get_chat_model(settings)` por um dublê sem SDK real -- é um bug de afinidade a event loop no SDK `openrouter`/`httpx` subjacente, não um bug deste projeto. Impacto: `main.py` (T25) agora constrói o modelo no `lifespan` de TODA app criada por `create_app()`, então qualquer teste de integração que chame `create_app()` sem passar `model=` explícito passa a rodar `ChatOpenRouter` real e pode travar o *próximo* teste async do processo (não necessariamente ele mesmo). Mitigação: `tests/integration/mcp_test_server.NeverInvokedChatModel` é o dublê padrão -- todo `create_app(...)` em teste que não exercita `POST /tasks` de propósito DEVE passar `model=NeverInvokedChatModel()` (aplicado em `test_main.py`/`test_routes_servers.py` durante T25); tasks futuras que chamem `create_app()` sem precisar do LLM real (T26 `GET /health`, testes de Docker/E2E que não sejam o smoke test) devem seguir o mesmo padrão. | active | `tests/integration/mcp_test_server.py` (`NeverInvokedChatModel`); `tests/integration/test_main.py`, `tests/integration/test_routes_servers.py` (uso do dublê); `src/orchestrator/main.py` (`create_app(model=...)`, T25) |
| AD-011 | Mesma classe de bug do AD-010, gatilho diferente: uma PRIMEIRA conexão MCP real que efetivamente INVOCA uma tool (não apenas `registry.discover()`/listagem, que é seguro -- `test_registry.py` já faz isso repetidamente sem problema) via `MultiServerMCPClient`/`langchain-mcp-adapters`, seguida por QUALQUER nova conexão MCP real (mesmo só um `discover()`) em um event loop asyncio diferente no mesmo processo, trava a segunda indefinidamente (mesmo sintoma: `ERROR: ASGI callable returned without completing response.`). Reproduzido isolado, fora do pytest, com script mínimo. Trocar `asyncio_default_test_loop_scope`/`asyncio_default_fixture_loop_scope` para `module` NÃO resolve (pytest-asyncio 1.4.0 continua criando um `asyncio.run()` novo por teste nesta config). Mitigação adotada: `main.py.create_app()` ganhou o parâmetro `tools_by_server` (T25) -- quando fornecido, `build_graph()` usa essas tools diretamente (dublês, sem conexão MCP real), no mesmo padrão de `tests/unit/test_builder.py`, contornando o SDK em vez de tentar consertá-lo. Regra para tasks futuras: no máximo UM teste por arquivo/processo pode fazer uma invocação de tool real via `MultiServerMCPClient`; qualquer outro teste que precise de um step real no trace deve usar `tools_by_server` com uma tool falsa (ver `_FakeReadFileTool` em `tests/integration/test_routes_tasks.py`) em vez de `run_fake_mcp_server` + `server_configs`. **Nota do AD-012**: essa regra continua valendo dentro de `tests/integration` sozinho (comprovadamente estável, 19/19 repetidas vezes), mas não foi suficiente para explicar o travamento AD-012 quando combinado com `tests/unit` -- ver AD-012. | active | `src/orchestrator/main.py` (`create_app(tools_by_server=...)`, T25); `tests/integration/test_routes_tasks.py` (`_FakeReadFileTool`, teste de timeout) |
| AD-012 | Rodar `tests/unit` e `tests/integration` **no mesmo comando/processo pytest** (`python -m pytest tests/unit tests/integration -q`, o comando que a tabela de Gate Check Commands definia para o gate `Full`/`Build`) trava de forma reprodutível -- mesmo sintoma de AD-010/AD-011 (`ERROR: ASGI callable returned without completing response.` + hang indefinido, preso em `select()`/`_poll()` esperando I/O que nunca chega). Encontrado durante T27 (Lote 4), ao adicionar os testes do shim. **Causa raiz NÃO identificada** apesar de bisseção extensa -- hipóteses eliminadas com evidência: (1) não é o shim (reproduzido removendo `test_shim.py` inteiramente); (2) não é AD-010/AD-011 clássico -- o teste que trava (`test_routes_servers.py::test_get_servers_reports_write_true_from_policy_not_the_registry_placeholder`) roda ANTES de qualquer teste de `test_routes_tasks.py` (a suposta "fonte" da poluição) na ordem de execução, então não é um caso de "invocação real anterior envenena a próxima"; (3) não é efeito de coleta/import -- rodar com `test_routes_tasks.py` coletado mas des-selecionado via `-k "not post_tasks"` passa limpo; (4) não é a política de event loop do Windows -- trocar `ProactorEventLoop` por `WindowsSelectorEventLoopPolicy` não resolveu (mesma trava, stack diferente); (5) não é lentidão -- com timeout de 280s o processo nunca termina sozinho. Cada suite sozinha é 100% confiável (`tests/unit`: 87 passed em ~2s; `tests/integration`: 19 passed em ~20s, repetido várias vezes). Decisão do usuário: em vez de continuar investigando, **redefinir o gate `Full`/`Build` como invocações de pytest separadas** (`python -m pytest tests/unit -q && python -m pytest tests/integration -q`, cada uma seu próprio processo) em vez de um comando combinado -- mesma cobertura, contorna o travamento. Regra para tasks futuras: NUNCA rodar `tests/unit` e `tests/integration` (nem `tests/eval`, quando existir) no mesmo comando `pytest`; sempre como comandos `&&` encadeados, cada um seu próprio processo. | active | `.specs/features/mcp-orchestrator/tasks.md` -> Gate Check Commands (linhas `Full`/`Build`), T36 (comando do Done-when corrigido) |
| AD-013 | `docker/filesystem/Dockerfile` e `docker/github/Dockerfile` (T29/T30) **não fazem mais `FROM shim-base:latest`** (uma tag local sem registry) -- viraram multi-stage AUTOCONTIDOS, cada um replicando os passos de `docker/shim-base/Dockerfile` (T28) como seu próprio primeiro stage. Motivo: tentei o mecanismo oficial do Docker Compose para isso (um serviço `shim-base` com `image: shim-base:latest` + `depends_on` + `additional_contexts: {shim-base:latest: service:shim-base}`, confirmado pela documentação oficial via Context7, com o bake JSON gerado corretamente linkando `contexts: {"shim-base:latest": "target:shim-base"}`) e não funcionou neste ambiente -- `docker compose build` seguia tentando fazer `pull` de `shim-base:latest` do Docker Hub em vez de usar o resultado do target `shim-base`, tanto com o driver `docker` padrão quanto criando um builder `docker-container` dedicado. Sem essa correção, `docker compose up -d` falharia em máquina limpa (spec.md P1, Independent Test) por falta da imagem base. `docker/shim-base/Dockerfile` (T28) continua existindo e buildável isoladamente (não foi removido), só deixou de ser uma dependência de build do compose. Encontrado e corrigido durante T33 (Lote 4), validando `docker compose build`/`up -d` de ponta a ponta pela primeira vez. | active | `docker/filesystem/Dockerfile`, `docker/github/Dockerfile` (T29/T30, multi-stage autocontidos); `docker-compose.yml` (T33, `context: .` para os dois, em vez de `docker/filesystem`/`docker/github`) |
| AD-014 | `mcp_client.registry.McpRegistry.discover()` (T8) travava indefinidamente contra um MCP server real com backend stdio de verdade (ex.: `mcp-filesystem` via `docker compose up -d`) -- o gateway nunca saía de "Waiting for application startup". Causa raiz isolada por bisseção empírica (subprocesso direto via stdio: ok; `asyncio.subprocess`+PIPE puro: ok; requisição HTTP crua via `httpx`: recebe os headers 200 OK com `content-type: text/event-stream` e `mcp-session-id`, mas a leitura do corpo trava). Com logging debug do SDK `mcp` ligado, confirmei que `sse_read_timeout` passado a `MultiServerMCPClient` **não é de fato aplicado** para essa situação (sessão aberta, headers entregues, primeiro evento SSE nunca chega) -- só um `asyncio.wait_for` EXTERNO à chamada consegue cancelar a leitura travada (testado com e sem: sem o wrapper externo, trava por 15s+ mesmo com `sse_read_timeout=5`; com o wrapper, cancela corretamente em ~5-8s). `github-mcp-server` (Go) não expôs esse sintoma porque falha rápido por credenciais inválidas antes de chegar nesse caminho -- só `mcp-server-filesystem` (Node, credenciais não exigidas) o expôs. Corrigido envolvendo `self._client.get_tools(server_name=cfg.name)` em `asyncio.wait_for(..., timeout=cfg.timeout)` dentro de `discover()`. Encontrado e corrigido durante T33 (Lote 4), validando `docker compose up -d` de ponta a ponta pela primeira vez contra os MCP servers reais -- nenhum teste da suíte pega isso porque todos usam `fastmcp.FastMCP` em processo (resposta instantânea, nunca exercita esse caminho de timeout). T8 foi retroativamente reaberta e corrigida; ver nota em `tasks.md` → T8. **Ver AD-015 para o achado seguinte, encontrado ao revalidar esta correção manualmente.** | active | `src/orchestrator/mcp_client/registry.py` (`discover()`, T8); `tests/integration/test_registry.py::test_discover_marks_a_server_whose_sse_stream_never_responds_as_unhealthy` (regressão); `tests/integration/mcp_test_server.py` (`sse_stream_hangs_server`) |
| AD-015 | Extensão do AD-014 -- mesmo arquivo (`registry.py`), mesma task (T8), causa raiz DIFERENTE. Depois de aplicar o fix do AD-014, o usuário revalidou `docker compose up -d` manualmente e encontrou `mcp-filesystem` ainda `unhealthy` mesmo com o startup completando dentro do `cfg.timeout` (30s) configurado -- os logs do server mostravam uma troca aparentemente bem-sucedida (POST/GET/POST/DELETE, todos 200/202), sem evidência de falha real. Investigação: `get_tools()` isolado contra o container real, chamado repetidamente, mostrou um padrão CLARO de corrida de largada -- a 1ª tentativa logo após o container `mcp-filesystem` subir (ou reiniciar) trava até `cfg.timeout`; a partir da 2ª tentativa (>=2s depois), sucesso instantâneo e consistente (confirmado 5/5 em múltiplas rodadas). Causa: o healthcheck TCP do compose (design.md Seção 5 -- "só verifica se o shim aceita conexão") marca o container `healthy` assim que o shim (uvicorn) sobe, mas o subprocesso stdio interno (`mcp-server-filesystem`) pode ainda não ter terminado de inicializar -- o `depends_on: condition: service_healthy` do gateway não cobre essa janela. Corrigido com 1 retry em `discover()` por server, com `_DISCOVER_RETRY_DELAY_S=1.0` de espera entre tentativas (mesmo padrão de retry único já usado em `graph.nodes.tools`, T21) -- decisão do usuário entre isso e enrijecer o healthcheck do compose para uma checagem MCP funcional real (rejeitada por divergir de design.md Seção 5, que delega a prova funcional real a `registry.py`, não ao compose). Trade-off aceito: quando a corrida acontece, o pior caso ainda leva até `cfg.timeout` completo (30s) antes do retry entrar -- validado contra a stack real repetidas vezes, sempre convergindo para `healthy` em ambos servers, só com essa latência ocasional. Teste de regressão (`test_discover_retries_once_and_recovers_from_a_flaky_first_attempt`) roda em SUBPROCESSO isolado (`registry_retry_check.py`) -- ver nota abaixo. | active | `src/orchestrator/mcp_client/registry.py` (`_discover_one`, `_DISCOVER_RETRY_DELAY_S`, T8); `tests/integration/registry_retry_check.py`, `tests/integration/test_registry.py::test_discover_retries_once_and_recovers_from_a_flaky_first_attempt`; `tests/integration/mcp_test_server.py` (`flaky_first_attempt_server`) |
| AD-016 | O teste de regressão do AD-015 precisou de isolamento em SUBPROCESSO, mais forte que o já estabelecido pelo AD-011. Tentativa inicial: rodar o teste in-process, como último teste do arquivo (mesma mitigação do AD-011, "1 invocação real por arquivo"). Resultado: mesmo como último teste do próprio arquivo, corrompeu testes de OUTROS arquivos de integração rodando depois no mesmo processo (`test_routes_health.py`, `test_routes_servers.py`, `test_routes_tasks.py`) -- a mitigação do AD-011 ("1 invocação real por arquivo") se mostrou insuficiente aqui porque a corrupção atravessa arquivos dentro do mesmo processo pytest, não só o próprio arquivo. Causa provável: este teste envolve uma conexão MCP real que é CANCELADA em pleno voo (a 1ª tentativa, forçadamente interrompida por `asyncio.wait_for`) -- um padrão mais agressivo que os triggers já catalogados no AD-010/AD-011 (que envolviam conexões bem-sucedidas ou nunca-iniciadas, não canceladas em progresso). Mitigado isolando o teste em subprocesso (`tests/integration/registry_retry_check.py`, mesmo padrão já usado em T27 para `test_shim.py`/`shim_translation_check.py`) -- confirmado com 5 execuções completas e limpas de `tests/integration` após o isolamento (antes: falha intermitente em arquivos downstream). Regra para tasks futuras: qualquer teste que envolva uma conexão MCP real CANCELADA em progresso (não só invocada com sucesso) deve rodar em subprocesso isolado por padrão, não só seguir a regra "1 por arquivo" do AD-011. | active | `tests/integration/registry_retry_check.py`; `tests/integration/test_registry.py::test_discover_retries_once_and_recovers_from_a_flaky_first_attempt` |
| AD-017 | `tests/eval/dataset.json` (T35) usa `list[str]` uniformemente para `expected_server`/`expected_tool` nos 15 casos, em vez dos campos singulares que `design.md` §6.1 mostra (`{"id", "task", "expected_server", "expected_tool", "expected_finish_reason"}`). Motivo: os 2 casos multi-step exigem expressar uma sequência ordenada em dois servers (`github` → `filesystem`); um campo singular não comporta isso. Decisão do usuário: em vez de alternar tipo por caso (string nos 13 casos de único server, lista só nos 2 multi-step), manter o mesmo tipo (`list[str]`) em todos os 15 -- lista de 1 elemento nos casos de único server, lista vazia nos 3 casos `no_suitable_server`. `design.md` §6.1 ainda não foi atualizado com o tipo de campo; fica para quando T36 (a suíte que consome este dataset) for implementada. | active | `tests/eval/dataset.json` (T35); `.specs/features/mcp-orchestrator/design.md` §6.1 (schema a complementar em T36) |

## Environment Notes

> Observações de ambiente de desenvolvimento/rede que não são decisões de arquitetura do
> projeto (por isso não numeradas como AD-xxx), mas que podem se repetir em sessões futuras e
> não devem ser re-diagnosticadas do zero como bug de código.

**EN-001 — Interceptação TLS corporativa (Netskope) bloqueia toda chamada HTTPS a
`openrouter.ai` nesta máquina/rede, dentro e fora do Docker.**

Durante a validação manual de T34 (smoke test) com credenciais OpenRouter reais,
`POST /tasks` travava por exatos `REQUEST_TIMEOUT_S` (120s) com `trace.iterations: 0` --
sintoma à primeira vista indistinguível de um bug em `orchestrator/llm/provider.py`
(`get_chat_model`/`ainvoke`) ou em como `graph/builder.py` aplica `bind_tools()` às tools MCP.

Causa raiz isolada com scripts mínimos fora do pytest (descartáveis, não versionados):
- Toda chamada HTTPS a `openrouter.ai` via `httpx`/`requests` (portanto também o SDK
  `openrouter` usado por `langchain-openrouter`) falha instantaneamente (~0.05s, nunca um
  timeout real de rede) com `ConnectError: [SSL: CERTIFICATE_VERIFY_FAILED] ... self-signed
  certificate in certificate chain`.
- O certificado de fato servido em `openrouter.ai:443` (confirmado via `ssl`/
  `openssl s_client`, fingerprint SHA-256
  `28c4ce8f2e9e0aa20bffac5f8282d232c923ddd45ed965e40431ff61a756b545`) é emitido por
  `ca.indragroup.goskope.com` (Netskope, CASB/SSE corporativo da rede da empresa) -- não pela
  CA real do site. É um proxy de inspeção TLS reassinando o tráfego.
- `curl` (usa o cert store do Windows via Schannel, não o bundle `certifi` do Python) passa da
  checagem de confiança -- a CA da Netskope já está instalada no Windows -- mas falha numa
  checagem de revogação (`CRYPT_E_NO_REVOCATION_CHECK`), etapa que o `ssl` do Python nem
  executa.
- **Confirmado também de DENTRO do container real** (`docker run --entrypoint python
  agent-orchestrator-mcp-gateway:latest`, a mesma imagem que `docker compose up -d` roda):
  mesmo fingerprint de certificado, mesma exceção `CERTIFICATE_VERIFY_FAILED` via
  `httpx.get(...)`. `docker info` mostra `HTTP Proxy`/`HTTPS Proxy:
  http.docker.internal:3128` -- o tráfego do container sai pelo mesmo caminho de rede do
  host, então a interceptação Netskope alcança igualmente o gateway rodando em
  `docker compose`. **Não é um problema específico de Python fora do Docker** -- o smoke
  test real dentro da stack (T34) sofre exatamente o mesmo bloqueio nesta rede.

O loop de retry embutido no SDK `openrouter` (`retry_connection_errors=True`) classifica esse
`ConnectError` como retentável e insiste com backoff exponencial contra um handshake que
nunca vai funcionar, até `asyncio.timeout(REQUEST_TIMEOUT_S)` em `routes_tasks.py` cortar em
120s -- daí o sintoma de "trava" em vez de falha imediata e visível.

**Decisão do usuário (2026-09-01):** nenhum workaround entra no código do repositório -- é
uma condição de rede/ambiente (proxy corporativo de inspeção TLS), não algo que
`llm/provider.py` deva contornar (o mesmo container se comporta de forma diferente fora desta
rede/VPN; um workaround aqui não deveria viver em código que também roda em CI/produção fora
dela). Antes de investigar qualquer timeout futuro de chamada HTTPS de saída (OpenRouter ou
qualquer outro serviço externo) como bug de aplicação, verificar primeiro o emissor do
certificado servido (`openssl s_client -connect <host>:443 -servername <host> | openssl x509
-noout -issuer`) -- se for `*.goskope.com`, é isto, não regressão de código.

## Handoff

**Feature ativa:** `mcp-orchestrator`
**Fase concluída:** Tasks (Fase 4 de 5 do `tlc-spec-driven`)
**Branch:** `docs/tasks-mcp-orchestrator`
**Artefatos desta fase:** `.specs/features/mcp-orchestrator/tasks.md` (novo — 36 tasks em 8 fases, Test Coverage Matrix, Gate Check Commands, diagramas por fase, 3 tabelas de checagem pré-aprovação); `.specs/features/mcp-orchestrator/spec.md` (emendado — tabela de Requirement Traceability ganhou coluna `Tasks`, MCPO-01..MCPO-11 em `In Tasks`, MCPO-12 em `Deferred`); `.specs/STATE.md` (este arquivo — AD-006 registrado).
**Próximo passo:** aguardar validação do usuário sobre `tasks.md`; ao aprovar, seguir para Fase 5 (Execute) com a oferta de sub-agentes em 5 lotes (F1+F2 · F3+F4 · F5 · F6+F7 · F8).
**Pendências levadas à Fase 5:** T1 confirma na prática o caminho exato do binário dentro da imagem `ghcr.io/github/github-mcp-server` antes de T30 depender dele; regravação de fixtures de avaliação (T36) é obrigatória sempre que `graph/prompts.py` mudar (AD-004).
